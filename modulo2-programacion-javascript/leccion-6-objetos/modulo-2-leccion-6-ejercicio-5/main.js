'use strict';

const inputText = document.querySelector('.js-inputText');

console.dir(inputText);

// Value es una propiedad de tipo string y por defecto esta vacia 
// por eso todo input se recoge como string.

// nodeName es una propiedad tipo string que nos muestra que etiqueta de html es 
// en este caso un input

// required es una propiedad tipo boolean que indica si es un input obligatorio o no
// en esye caso nos devuelve un false (no es obligatorio)