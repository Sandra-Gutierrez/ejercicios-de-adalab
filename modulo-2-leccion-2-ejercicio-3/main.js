'use strict';

const kiwisKg = 2;
const kiwisPrice = 5;
const totalKiwis = kiwisKg * kiwisPrice;

const perasKg = 3;
const perasPrice = 2;
const totalPeras = perasKg * perasPrice;

const uvasKg = 0.5;
const uvasPrice = 4;
const totalUvas = uvasKg * uvasPrice;

const total = totalKiwis + totalPeras + totalUvas;
console.log(total);