'use strict';

const numPerson = 9;
const totalPrice = 128;
const sake = 2;
const dinnerPrice = totalPrice - sake;
console.log(dinnerPrice);
const divPrice = dinnerPrice / numPerson;
console.log(divPrice);
const anaPrice = divPrice + sake;
console.log(anaPrice);
